define(['appModule'], function(app) {
  app.lazy.controller('GameController',
    [
      '$scope',
      '$http',
      '$cookies',

      function($scope, $http, $cookie)
      {

      }
    ]
  );
});